export function pencapian(target, actual) {
    // const number = ac
    // const number = ((actual / (target)) * (100/100))
    // let persentase = actual / target;
    // console.log(persentase)
    let formatPersentase = ((actual / target) * 100);
    return formatPersentase;
    // return number * 100;
    //return ((actual / (target)) * (100/100));
}